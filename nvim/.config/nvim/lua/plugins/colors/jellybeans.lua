return {
  "wtfox/jellybeans.nvim",
  -- dir = "~/dev/jellybeans.nvim",
  opts = {
    italics = false,
    -- on_colors = function(c)
    -- end,

    -- on_highlights = function(hl, c)
    -- end,
  },
}
